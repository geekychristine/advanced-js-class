import {
  inventors,
  people,
  data,
  inventorsBirthYear,
  inventorNames,
  inventorBirthYears,
  inventorTotalYears,
  inventorYearsLived,
  parisianBoulevards,
  peopleSorted,
  dataInstances,
} from "./arrays";

describe("Inventors", () => {
  test("List inventors born in the 1500's", () => {
    expect(inventorsBirthYear()).toEqual();
  });
});
